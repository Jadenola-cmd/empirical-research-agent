# 2026-06-30 工作日志

## 修复：实证分析报告三项格式问题

### 用户反馈的三个问题
1. 回归表格式错误：系数/t值/显著性分三列，不符合期刊标准
2. analysis.py 完全绕过 engine/ 脚本，自己写了 statsmodels/linearmodels 代码
3. 相关系数矩阵为全矩阵，无显著性标注，不符合论文格式

### 修复内容

#### 1. 重写 analysis_v2.py — 真正调用 engine/ 脚本
- 架构：数据合并预处理 → 保存 parquet → subprocess 调用 engine/*.py → 汇总 all_results.json
- 每个计算步骤均通过 engine/descriptive.py / correlation.py / ols.py / panel.py / diagnostics.py 执行
- diagnostics 参数名 bug：config 中应用 dv/iv/controls，而非 dep_var/indep_vars/control_vars

#### 2. 修复 engine/ 中的 bug
- engine/descriptive.py: `columns=columns` → `numeric_cols=columns`（与 stats_core 签名不一致）
- engine/correlation.py: 同上
- 安装缺失依赖：pyarrow（用于 parquet 读写）

#### 3. Hausman 检验：已通过 stats_core.run_panel 内置逻辑成功运行
- chi2=7.686, p=0.566, 结论：不拒绝随机效应

#### 4. 新报告格式（资本结构影响因素_实证分析报告_v2.html）
- 回归表：系数+星号同行，下行括号内为 t 值（期刊标准格式）
- 相关系数矩阵：下三角+系数旁显著性星号，上三角显示"—"，对角线=1，编号列头
- OLS/FE/RE 三列并排
- 所有数字来自 engine 输出的 all_results.json，无心算

### 主要结果更新
- N=150（比之前的162少，因 parquet dropna 策略不同）
- Hausman: p=0.566，不拒绝随机效应（以RE为主要解读依据）
- ROA: RE β=-0.328** 显著；TURN: RE β=0.079* 显著；TANG: RE β=0.294* 边际显著

### 输出文件
- `AI_Output/WorkBuddy/资本结构影响因素_实证分析报告_v2.html`
- `analysis_v2.py`（调用 engine 的正确版本）
- `panel_data.parquet`、`all_results.json`
